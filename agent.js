export const agentFunction = (pageHTML, pageCSS, messageText) => {
  // TODO: Implement your agent logic here
  // This function receives the page HTML, CSS, and user message
  // and should return the CSS changes to apply

  console.log('Page HTML:', pageHTML);
  console.log('Page CSS:', pageCSS);
  console.log('Message:', messageText);

  // Placeholder: return some CSS
  return `
    body {
      /* Add your CSS modifications here */
    }
  `;
};
